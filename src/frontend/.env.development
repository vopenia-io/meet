# For LAN access, leave VITE_API_BASE_URL empty so frontend uses window.location.origin
# which goes through the nginx-lan proxy at https://192.168.0.10
# VITE_API_BASE_URL=http://localhost:8071/
VITE_APP_TITLE=LaSuite Meet
